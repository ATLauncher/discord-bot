import BaseModule from '../BaseModule';

/**
 * This is the base command class which all other commands are based on.l
 *
 * @class BaseCommand
 * @extends {BaseModule}
 */
class BaseCommand extends BaseModule {}

export default BaseCommand;
